export type Category = { id: string; name: string; slug: string; description: string | null; published: boolean; sort_order: number };
export type ProductImage = { id: string; product_id: string; storage_path: string; public_url: string; alt_text: string | null; sort_order: number; is_cover: boolean };
export type ProductVariant = { id: string; product_id: string; name: string; value: string; stock: number | null; price_adjustment: number; sort_order: number };
export type Product = { id: string; name: string; slug: string; short_description: string | null; description: string | null; price: number; compare_at_price: number | null; stock: number; category_id: string | null; published: boolean; featured: boolean; is_new: boolean; on_sale: boolean; is_unique: boolean; customizable: boolean; created_at: string; updated_at: string; category?: Category | null; images?: ProductImage[]; variants?: ProductVariant[] };
export type StoreSettings = { store_name: string; slogan: string; whatsapp: string; instagram: string | null; tiktok: string | null; facebook: string | null; email: string | null; banner_url: string | null; about_text: string | null; shipping_info: string | null; contact_info: string | null };
export type CartItem = { key: string; productId: string; slug: string; name: string; image: string | null; price: number; quantity: number; stock: number; variant: string | null };
