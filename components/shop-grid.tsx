"use client";import{useMemo,useState}from"react";import type{Category,Product}from"@/types/database";import{ProductCard}from"./product-card";
export function ShopGrid({products,categories}:{products:Product[];categories:Category[]}){const[q,setQ]=useState(""),[category,setCategory]=useState(""),[available,setAvailable]=useState("all"),[sort,setSort]=useState("recent");const filtered=useMemo(()=>products.filter(p=>(!q||`${p.name} ${p.description}`.toLowerCase().includes(q.toLowerCase()))&&(!category||p.category_id===category)&&(available==="all"||(available==="yes"?p.stock>0:p.stock===0))).sort((a,b)=>sort==="low"?a.price-b.price:sort==="high"?b.price-a.price:sort==="featured"?Number(b.featured)-Number(a.featured):new Date(b.created_at).getTime()-new Date(a.created_at).getTime()),[products,q,category,available,sort]);return <><div className="filters"><input className="field" placeholder="Buscar por nombre o detalle…" value={q} onChange={e=>setQ(e.target.value)}/><select className="field" value={category} onChange={e=>setCategory(e.target.value)}><option value="">Todas las categorías</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select className="field" value={available} onChange={e=>setAvailable(e.target.value)}><option value="all">Toda disponibilidad</option><option value="yes">Disponible</option><option value="no">Agotado</option></select><select className="field" value={sort} onChange={e=>setSort(e.target.value)}><option value="recent">Más recientes</option><option value="low">Menor precio</option><option value="high">Mayor precio</option><option value="featured">Destacados</option></select></div>{filtered.length?<div className="product-grid">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty"><h2>No encontramos piezas</h2><p>Prueba con otros filtros o vuelve pronto.</p></div>}</>}
