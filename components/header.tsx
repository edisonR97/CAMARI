"use client";import Link from"next/link";import{useState}from"react";import{Logo}from"./logo";import{BagIcon,MenuIcon,SearchIcon}from"./icons";import{useCart}from"./cart-provider";const links=[["Inicio","/"],["Tienda","/tienda"],["Colecciones","/tienda#colecciones"],["Personalizados","/personalizados"],["Nosotros","/nosotros"],["Contacto","/contacto"]];export function Header(){const[open,setOpen]=useState(false);const{count}=useCart();return <header className="site-header"><div className="shell header-inner"><Logo/><nav className={open?"nav open":"nav"}>{links.map(([l,h])=><Link key={h+l} href={h} onClick={()=>setOpen(false)}>{l}</Link>)}</nav><div className="header-actions"><Link href="/tienda" aria-label="Buscar"><SearchIcon/></Link><Link href="/carrito" aria-label={`Carrito, ${count} productos`} className="cart-link"><BagIcon/>{count>0&&<b>{count}</b>}</Link><button onClick={()=>setOpen(!open)} className="menu-button" aria-label="Abrir menú"><MenuIcon/></button></div></div></header>}
