import type{MetadataRoute}from"next";import{getProducts}from"@/lib/data";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const products=await getProducts();return["","/tienda","/personalizados","/nosotros","/contacto"].map(url=>({url:base+url,lastModified:new Date()})).concat(products.map(p=>({url:`${base}/producto/${p.slug}`,lastModified:new Date(p.updated_at)})))}
