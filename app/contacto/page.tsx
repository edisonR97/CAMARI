import type{Metadata}from"next";import{getSettings}from"@/lib/data";export const metadata:Metadata={title:"Contacto"};export default async function Contact(){const s=await getSettings();return <><header className="page-hero shell"><span className="eyebrow">Hablemos</span><h1>Estamos cerca de ti</h1><p className="muted">Una pregunta, una idea o un regalo especial: escríbenos.</p></header><section className="section shell" style={{paddingTop:20,maxWidth:850}}><form className="panel form-grid"><label className="label">Nombre<input className="field" required/></label><label className="label">Correo<input className="field" type="email" required/></label><label className="label">Teléfono<input className="field" type="tel"/></label><label className="label full">Mensaje<textarea className="field" rows={5} required/></label><button className="btn full">Enviar mensaje</button></form><div style={{textAlign:"center",marginTop:28}}>{s.email&&<a href={`mailto:${s.email}`}>{s.email}</a>}</div></section></>}
