import type{Metadata}from"next";import{getSettings}from"@/lib/data";import{WhatsAppForm}from"@/components/whatsapp-form";export const metadata:Metadata={title:"Personalizados"};export default async function Custom(){const s=await getSettings();return <><header className="page-hero shell"><span className="eyebrow">Creado contigo</span><h1>Hazlo tuyo</h1><p className="muted">Cuéntanos tu idea y crearemos una pieza especialmente para ti.</p></header><section className="section shell" style={{paddingTop:20,maxWidth:850}}><WhatsAppForm number={s.whatsapp}/></section></>}
