import type{Metadata}from"next";import{getCategories,getProducts}from"@/lib/data";import{ShopGrid}from"@/components/shop-grid";export const metadata:Metadata={title:"Tienda"};export default async function Shop(){const[products,categories]=await Promise.all([getProducts(),getCategories()]);return <><header className="page-hero shell"><span className="eyebrow">Piezas con identidad</span><h1>Tienda CAMARI</h1><p className="muted">Encuentra ese detalle que habla de ti.</p></header><section className="shell section" style={{paddingTop:20}}><ShopGrid products={products} categories={categories}/></section></>}
