import{requireAdmin}from"@/lib/admin";export default async function Orders(){const{s}=await requireAdmin();const{data}=await s.from("orders").select("*").order("created_at",{ascending:false});return <><span className="eyebrow">Seguimiento</span><h1 style={{fontSize:52}}>Pedidos</h1><div className="panel">{data?.length?data.map(o=><div key={o.id} style={{padding:14,borderBottom:"1px solid #eee"}}><b>{o.customer_name}</b> · {o.status} · {o.total}</div>):<div className="empty">No hay pedidos registrados.</div>}</div></>}
