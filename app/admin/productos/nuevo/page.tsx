import{requireAdmin}from"@/lib/admin";import{ProductForm}from"@/components/product-form";import type{Category}from"@/types/database";export default async function NewProduct(){const{s}=await requireAdmin();const{data}=await s.from("categories").select("*").order("sort_order");return <><span className="eyebrow">Catálogo</span><h1 style={{fontSize:52}}>Nuevo producto</h1><ProductForm categories={(data??[])as Category[]}/></>}
