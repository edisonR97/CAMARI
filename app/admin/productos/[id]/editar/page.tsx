import{notFound}from"next/navigation";import{requireAdmin}from"@/lib/admin";import{ProductForm}from"@/components/product-form";import type{Category,Product}from"@/types/database";export default async function Edit({params}:{params:Promise<{id:string}>}){const{id}=await params;const{s}=await requireAdmin();const[{data:p},{data:c}]=await Promise.all([s.from("products").select("*, images:product_images(*), variants:product_variants(*)").eq("id",id).single(),s.from("categories").select("*").order("sort_order")]);if(!p)notFound();return <><span className="eyebrow">Catálogo</span><h1 style={{fontSize:52}}>Editar producto</h1><ProductForm product={p as Product} categories={(c??[])as Category[]}/></>}
