import{requireAdmin}from"@/lib/admin";export default async function Gallery(){const{s}=await requireAdmin();const{data}=await s.from("gallery").select("*").order("sort_order");return <><span className="eyebrow">Contenido visual</span><h1 style={{fontSize:52}}>Galería</h1><div className="panel"><p>{data?.length??0} elementos en la galería.</p><p className="muted">Administra fotografías públicas desde el bucket <b>gallery</b> de Supabase.</p></div></>}
