import{requireAdmin}from"@/lib/admin";export default async function Testimonials(){const{s}=await requireAdmin();const{data}=await s.from("testimonials").select("*").order("created_at",{ascending:false});return <><span className="eyebrow">Comunidad</span><h1 style={{fontSize:52}}>Testimonios</h1><div className="panel">{data?.length?data.map(t=><div key={t.id} style={{padding:14,borderBottom:"1px solid #eee"}}><b>{t.name}</b> · {"★".repeat(t.rating)}<p>{t.comment}</p></div>):<div className="empty">No hay testimonios.</div>}</div></>}
