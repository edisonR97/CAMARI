import{requireAdmin}from"@/lib/admin";export default async function Requests(){const{s}=await requireAdmin();const{data}=await s.from("custom_requests").select("*").order("created_at",{ascending:false});return <><span className="eyebrow">Solicitudes</span><h1 style={{fontSize:52}}>Personalizados</h1><div className="panel">{data?.length?data.map(r=><div key={r.id} style={{padding:14,borderBottom:"1px solid #eee"}}><b>{r.name}</b> · {r.product_type}<p>{r.description}</p></div>):<div className="empty">No hay solicitudes registradas.</div>}</div></>}
