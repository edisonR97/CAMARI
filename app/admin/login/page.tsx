"use client";import Link from"next/link";import{useState}from"react";import{useRouter}from"next/navigation";import{createClient}from"@/lib/supabase/client";import{Logo}from"@/components/logo";export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);const router=useRouter();return <section className="admin-login"><div className="login-story"><Logo light/><div><span className="eyebrow">CAMARI · Gestión</span><h1>Tu creatividad,<br/>bien administrada.</h1><p>Controla el catálogo, las fotografías y cada detalle de la tienda desde un espacio privado.</p></div><small>Arte hecho para conectar.</small></div><form className="login-card" onSubmit={async e=>{e.preventDefault();setLoading(true);setError("");try{const{error}=await createClient().auth.signInWithPassword({email,password});if(error)throw error;router.push("/admin");router.refresh()}catch(e){setError(e instanceof Error?e.message:"No fue posible iniciar sesión")}finally{setLoading(false)}}}><Link href="/" className="back-link">← Volver a la tienda</Link><span className="eyebrow">Área privada</span><h2>Bienvenido</h2><p className="muted">Ingresa con tu cuenta de administrador.</p><label className="label">Correo electrónico<input className="field" type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="correo@camari.com"/></label><label className="label">Contraseña<input className="field" type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label>{error&&<p className="form-error" role="alert">{error}</p>}<button className="btn" disabled={loading}>{loading?"Ingresando…":"Ingresar al panel"}</button><small className="muted">Acceso exclusivo para personal autorizado.</small></form></section>}
