"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{createClient}from"@/lib/supabase/client";export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);const router=useRouter();return <section className="section shell" style={{maxWidth:500}}><form className="panel" onSubmit={async e=>{e.preventDefault();setLoading(true);setError("");try{const{error}=await createClient().auth.signInWithPassword({email,password});if(error)throw error;router.push("/admin");router.refresh()}catch(e){setError(e instanceof Error?e.message:"No fue posible iniciar sesión")}finally{setLoading(false)}}}><span className="eyebrow">Área privada</span><h1 style={{fontSize:48}}>Administración</h1><label className="label">Correo<input className="field" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="label" style={{marginTop:16}}>Contraseña<input className="field" type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p style={{color:"#a33"}}>{error}</p>}<button className="btn" style={{width:"100%",marginTop:22}} disabled={loading}>{loading?"Ingresando…":"Iniciar sesión"}</button></form></section>}
