import type{Metadata}from"next";import"./globals.css";import{CartProvider}from"@/components/cart-provider";import{Header}from"@/components/header";import{Footer}from"@/components/footer";import{getSettings}from"@/lib/data";
export const metadata:Metadata={metadataBase:new URL(process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000"),title:{default:"CAMARI | Arte hecho para conectar",template:"%s | CAMARI"},description:"Artesanías hechas a mano con color, creatividad y esencia.",openGraph:{title:"CAMARI",description:"Arte hecho para conectar.",type:"website"}};
export default async function RootLayout({children}:{children:React.ReactNode}){const settings=await getSettings();return <html lang="es"><body><CartProvider><Header/><main>{children}</main><Footer settings={settings}/></CartProvider></body></html>}
