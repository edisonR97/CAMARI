import { createBrowserClient } from "@supabase/ssr";
export function createClient(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL?.trim();const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY?.trim();if(!url||!key)throw new Error("Faltan las variables públicas de Supabase");try{const parsed=new URL(url);if(!["http:","https:"].includes(parsed.protocol))throw new Error()}catch{throw new Error("NEXT_PUBLIC_SUPABASE_URL debe ser una URL válida de Supabase")};return createBrowserClient(url,key)}
